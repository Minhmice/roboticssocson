# Legacy archive — not routed, not imported by the active app

Frozen snapshots of the former `/sponsor` and `/sponsorship` product surfaces.

- `sponsor/` — old App Router pages for `/sponsor`
- `sponsorship/` — old App Router pages for `/sponsorship`
- `components-sponsor/` — former `src/components/sponsor/*`
- `*.ts` / `BudgetTable.tsx` / etc. — sponsor-only data and UI helpers

Do not re-add these under `src/app/`. Restore intentionally if sponsorship returns.
