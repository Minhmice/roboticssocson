---
phase: 3
plan: "03"
type: execute
wave: 2
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - "src/app/course/page.tsx"
  - "src/app/course/CourseHero.tsx"
autonomous: true
requirements:
  - NAV-01
  - I18N-01
  - QUAL-01
---

# Plan 03-03: Course Route Shell and Hero

## must_haves

- D-ROUTE-01: /course route at src/app/course/page.tsx + layout.tsx
- D-UI-01: MediaPlaceholder without real /Images/Course/ asset
- D-UI-02: AuroraBackground, CTAButton, getLocalized for hero copy

<objective>
Ship live `/course` route with `CourseHero` composing `AuroraBackground`, data-driven bilingual copy, and placeholder visual — proving route + data + SEO integration before Phase 4 sections.
</objective>

## Context Coverage

- D-ROUTE-01: `/course` slug
- D-UI-01: `MediaPlaceholder` without real image path
- D-UI-02: Reuse `AuroraBackground`, `CTAButton`, `getLocalized`
- NAV-01: dedicated course page at `/course`
- I18N-01: language toggle switches hero copy
- Phase 3 does NOT render curriculum UI or change Navbar (Phase 5)

## Artifacts this phase produces

- `src/app/course/CourseHero.tsx` — client hero section
- `src/app/course/page.tsx` — composes `CourseHero` only

<tasks>

<task id="01" type="execute">
  <title>Implement CourseHero component</title>
  <read_first>
    <file>src/components/homepage/Hero.tsx</file>
    <file>src/data/courseHero.ts</file>
    <file>src/lib/course/getLocalized.ts</file>
    <file>src/components/shared/MediaPlaceholder.tsx</file>
    <file>src/components/shared/CTAButton.tsx</file>
    <file>DESIGN.md</file>
  </read_first>
  <action>
    Create `src/app/course/CourseHero.tsx` as `"use client"` component:
    - Import `courseHeroData` from `@/data/courseHero`
    - Import `getLocalized` from `@/lib/course/getLocalized`
    - Import `useLanguage` from `@/contexts/LanguageContext`
    - Wrap content in `AuroraBackground` with `className="bg-background text-foreground"`
    - Container: `max-w-7xl mx-auto px-4 sm:px-6 md:px-8 py-12 sm:py-16 md:py-24`
  Layout: two-column on `lg:` — left column copy, right column visual.
    - Single `h1` with `getLocalized(courseHeroData.headline, locale)`
    - Subtitle paragraph from `courseHeroData.subtitle`
    - Badge row from `courseHeroData.badges` (small pill chips)
    - Two CTAs using `CTAButton` or `GlassButton` pattern from homepage Hero:
      - Primary: `href="#course-curriculum"` label from `ctaPrimary` (anchor for Phase 4; OK if section not rendered yet)
      - Secondary: `href="#course-register"` label from `ctaSecondary`
    - Right column: `MediaPlaceholder` with **no** `src` prop — gradient/icon treatment only (D-UI-01); do not reference `/Images/Course/` paths
    Match dark arena tokens: `text-foreground`, `text-primary` accents, min 44px touch targets on CTAs.
  </action>
  <verify>
    <automated>test -f src/app/course/CourseHero.tsx</automated>
    <automated>rg "use client" src/app/course/CourseHero.tsx</automated>
    <automated>rg "courseHeroData" src/app/course/CourseHero.tsx</automated>
    <automated>rg "/Images/Course" src/app/course/CourseHero.tsx ; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`CourseHero` renders one `h1` and uses `getLocalized` with `useLanguage().locale`.</criterion>
    <criterion>Hero uses `MediaPlaceholder` without a real image asset path.</criterion>
    <criterion>Component follows `AuroraBackground` + spacing conventions from existing Hero.</criterion>
  </acceptance_criteria>
</task>

<task id="02" type="execute">
  <title>Wire course page and verify route</title>
  <read_first>
    <file>src/app/course/layout.tsx</file>
    <file>src/app/course/CourseHero.tsx</file>
    <file>src/app/sponsor/page.tsx</file>
    <file>docs/course/ACCEPTANCE_CRITERIA.md</file>
  </read_first>
  <action>
    Create `src/app/course/page.tsx` as `"use client"` thin composer importing and rendering only `CourseHero` (Phase 4 adds remaining sections).
    Do NOT modify `Navbar.tsx`, `Footer.tsx`, or `src/app/page.tsx` in this plan (deferred Phase 5).
    Run `npm run build` and `npm run lint`.
    Manually verify: `curl -s http://localhost:4000/course | rg "Từ Khối Lệnh|From Blocks"` OR inspect build output for `/course` static route.
  </action>
  <verify>
    <automated>test -f src/app/course/page.tsx</automated>
    <automated>npm run build</automated>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`/course` route exists at `src/app/course/page.tsx` and builds successfully (NAV-01).</criterion>
    <criterion>Build and lint pass with no new errors in course files.</criterion>
    <criterion>Club routes `/`, `/sponsorship`, `/sponsor` source files are untouched by this plan.</criterion>
  </acceptance_criteria>
</task>

<task id="03" type="execute">
  <title>Verify bilingual hero and SEO regression</title>
  <read_first>
    <file>src/contexts/LanguageContext.tsx</file>
    <file>src/lib/seo/course-seo.ts</file>
    <file>.planning/phases/03-course-data-route-foundation/03-CONTEXT.md</file>
  </read_first>
  <action>
    Confirm I18N-01 for Phase 3 scope:
    - Toggle language in browser on `/course`; hero headline/subtitle/CTAs switch between vi and en via `getLocalized` + `courseHeroData`.
    Confirm QUAL-01:
    - View page source or `curl` `/course` HTML; `&lt;title&gt;` contains course title string from `courseSeo.vi.title`, not only generic "RBS - Sponsorship Site" without course name.
    - Confirm no `document.title` assignment in course components (`rg "document.title" src/app/course/` should exit non-zero).
    Document results in plan execution notes if automated curl unavailable.
  </action>
  <verify>
    <automated>rg "document.title" src/app/course/ ; test $? -ne 0</automated>
    <automated>rg "useDynamicMetadata" src/app/course/ ; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    <criterion>Language toggle changes hero text between Vietnamese and English.</criterion>
    <criterion>Server-rendered `/course` title is course-specific per `courseSeo.vi`.</criterion>
    <criterion>No client-side SEO patching on `/course`.</criterion>
  </acceptance_criteria>
</task>

</tasks>

<must_haves>
<truths>
- D-ROUTE-01: Live route at `/course` via `page.tsx` + `layout.tsx`.
- D-UI-01: `CourseHero` uses `MediaPlaceholder` without `/Images/Course/` asset.
- D-UI-02: Hero reuses `AuroraBackground`, `CTAButton`, and `getLocalized` for copy.
- NAV-01: User can open dedicated course page at `/course`.
- I18N-01: Language toggle switches hero copy between vi and en.
- QUAL-01: `/course` has dedicated server metadata (title, description, Open Graph).
- `npm run build` and `npm run lint` pass.
- Existing club and sponsor pages are not regressed by Phase 3 file changes.
</truths>
<prohibitions>
- statement: Do not add navbar course link or homepage teaser in Phase 3
  status: resolved
  verification: `rg "nav.course|CourseTeaser" src/components/layout/Navbar.tsx src/app/page.tsx` shows no new course nav/teaser changes from this phase
</prohibitions>
</must_haves>

## PLAN COMPLETE
