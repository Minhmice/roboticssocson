---
phase: 3
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - "src/lib/course/getLocalized.ts"
  - "src/data/courseHero.ts"
  - "src/data/courseCurriculum.ts"
  - "src/data/courseProjects.ts"
  - "src/data/courseFaq.ts"
  - "src/data/courseRegistration.ts"
  - "src/data/courseSections.ts"
  - "src/data/index.ts"
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - I18N-01
---

# Plan 03-02: Course Static Data Layer

## must_haves

- D-DATA-01: Split courseHero, courseCurriculum, courseProjects, courseFaq, courseRegistration, courseSections
- D-DATA-02: Inline types per data file; no src/types/course.ts
- D-DATA-03: Export all course modules from src/data/index.ts
- D-CURR-01: Exactly 12 lessons in courseCurriculum.ts
- D-CURR-02: LocalizedText nested bilingual fields
- D-CURR-03: CourseLesson contract fields complete per lesson
- D-CURR-04: No placeholder strings in curriculum
- D-CURR-05: Data only — no CourseCurriculum UI in Phase 3

<objective>
Add typed bilingual course data modules under `src/data/course*.ts` with inline types, a `LocalizedText` helper for nested i18n fields, 12 complete curriculum lessons, and compile-safe stubs for Phase 4 sections.
</objective>

## Context Coverage

- D-DATA-01: Split data files per technical spec
- D-DATA-02: Types inline in each data file
- D-DATA-03: Export from `src/data/index.ts`
- D-CURR-01 through D-CURR-05: 12 lessons, `LocalizedText`, no placeholders, no curriculum UI
- Delta from `DATA_CONTRACT.md`: curriculum uses nested `LocalizedText` instead of `_vi`/`_en` suffixes (documented intentional)

## truths

- D-DATA-01: Split course data files per technical spec.
- D-DATA-02: Types inline in each data file; no `src/types/course.ts`.
- D-DATA-03: Export new modules from `src/data/index.ts`.
- D-CURR-01: `courseCurriculum.ts` exports exactly 12 lessons.
- D-CURR-02: Use nested `LocalizedText` for course domain fields.
- D-CURR-03: Each lesson satisfies full `CourseLesson` contract.
- D-CURR-04: No placeholder content in curriculum data.
- D-CURR-05: Phase 3 creates data only; no curriculum UI.

## Artifacts this phase produces

- `src/lib/course/getLocalized.ts` — `LocalizedText`, `getLocalized(text, locale)`
- `src/data/courseHero.ts` — hero copy with `LocalizedText` fields
- `src/data/courseCurriculum.ts` — `courseLessons` array length 12, `CourseLesson` type
- `src/data/courseProjects.ts` — typed `courseProjects: Project[]` (empty array stub)
- `src/data/courseFaq.ts` — typed `courseFaq: FAQItem[]` (empty array stub)
- `src/data/courseRegistration.ts` — typed `courseRegistrationConfig` stub
- `src/data/courseSections.ts` — typed section blobs (empty arrays)
- `src/data/index.ts` — re-exports all course modules

<tasks>

<task id="01" type="execute">
  <title>Create LocalizedText helper</title>
  <read_first>
    <file>src/hooks/useTranslatedData.ts</file>
    <file>.planning/phases/03-course-data-route-foundation/03-CONTEXT.md</file>
    <file>docs/course/DATA_CONTRACT.md</file>
  </read_first>
  <action>
    Create `src/lib/course/getLocalized.ts` exporting:
    ```ts
    export type LocalizedText = { vi: string; en: string };
    export type CourseLocale = "vi" | "en";
    export function getLocalized(text: LocalizedText, locale: CourseLocale): string;
    ```
    `getLocalized` returns `text[locale]` falling back to `text.vi` if missing.
    No React imports — safe for server and client.
  </action>
  <verify>
    <automated>test -f src/lib/course/getLocalized.ts</automated>
    <automated>rg "export type LocalizedText" src/lib/course/getLocalized.ts</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`getLocalized.ts` exports `LocalizedText`, `CourseLocale`, and `getLocalized` without `"use client"`.</criterion>
  </acceptance_criteria>
</task>

<task id="02" type="execute">
  <title>Create courseHero data module</title>
  <read_first>
    <file>src/data/hero.ts</file>
    <file>docs/course/PRD.md</file>
    <file>src/lib/course/getLocalized.ts</file>
  </read_first>
  <action>
    Create `src/data/courseHero.ts` with inline types and `export const courseHeroData` containing `LocalizedText` fields:
    - `badge` — e.g. vi: "Dành cho học sinh cấp 1–2", en equivalent
    - `headline` — vi: "Từ Khối Lệnh Đến Phần Cứng", en: "From Blocks to Hardware"
    - `subtitle` — Scratch → flowchart → mBlock → Arduino journey (1 sentence each locale)
    - `ctaPrimary` — vi: "Xem lộ trình", en: "View curriculum"
    - `ctaSecondary` — vi: "Đăng ký tư vấn", en: "Register for consultation"
    - `badges` — readonly array of 3 `LocalizedText` items matching PRD hero badges (grades 1–2, 12 sessions, hands-on)
    All strings non-empty, bilingual, no placeholders.
  </action>
  <verify>
    <automated>test -f src/data/courseHero.ts</automated>
    <automated>rg "export const courseHeroData" src/data/courseHero.ts</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`courseHeroData` exports headline, subtitle, CTAs, and badges with both `vi` and `en` on every `LocalizedText` field.</criterion>
  </acceptance_criteria>
</task>

<task id="03" type="execute">
  <title>Create courseCurriculum with 12 complete lessons</title>
  <read_first>
    <file>docs/course/PRD.md</file>
    <file>docs/course/DATA_CONTRACT.md</file>
    <file>.planning/phases/01-shape-curriculum-experience/01-SHAPE-BRIEF.md</file>
    <file>src/lib/course/getLocalized.ts</file>
  </read_first>
  <action>
    Create `src/data/courseCurriculum.ts` with inline types:
    ```ts
    export type CourseLesson = {
      id: number; // 1–12
      part: "scratch" | "hardware" | "capstone";
      title: LocalizedText;
      goal: LocalizedText;
      product: LocalizedText;
      primaryLevel: LocalizedText;
      secondaryLevel: LocalizedText;
      challenge: LocalizedText;
    };
    export const courseLessons: readonly CourseLesson[];
    ```
    Export **exactly 12** lessons with `id` 1–12 and `part` mapping per DATA_CONTRACT §3.2 table:
    1–6 scratch, 7–11 hardware, 12 capstone.
    Titles (vi) from PRD: Làm quen Scratch, Lệnh tuần tự, Vòng lặp forever, Repeat + animation, If/else + va chạm, Biến điểm số + game mini, Điện cơ bản, LED chớp, Đèn giao thông, Nút nhấn, Cảm biến siêu âm + servo, Dự án cuối khóa.
    Each lesson: concise bilingual `goal`, `product`, `primaryLevel` (cấp 1 focus), `secondaryLevel` (cấp 2 extension), `challenge` — 1–2 sentences per field max.
    Example lesson 1 product vi: "Nhân vật nói xin chào và di chuyển"; challenge vi: "Tạo đoạn hội thoại giữa hai nhân vật".
    **Forbidden:** TBD, lorem ipsum, coming soon, empty strings in any `vi`/`en` field.
  </action>
  <verify>
    <automated>node -e "const m=require('./src/data/courseCurriculum.ts');" 2>/dev/null || npx tsc --noEmit 2>/dev/null; rg "export const courseLessons" src/data/courseCurriculum.ts</automated>
    <automated>rg -c "id: [0-9]+" src/data/courseCurriculum.ts | head -1</automated>
    <automated>rg "TBD|lorem|coming soon" src/data/courseCurriculum.ts ; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`courseLessons` array has exactly 12 entries with ids 1 through 12.</criterion>
    <criterion>Every lesson has `title`, `goal`, `product`, `primaryLevel`, `secondaryLevel`, `challenge` as `LocalizedText` with both `vi` and `en` populated.</criterion>
    <criterion>No placeholder content in curriculum data.</criterion>
  </acceptance_criteria>
</task>

<task id="04" type="execute">
  <title>Create Phase 4 data stubs and index exports</title>
  <read_first>
    <file>docs/course/DATA_CONTRACT.md</file>
    <file>src/data/sponsorPage.ts</file>
    <file>src/data/index.ts</file>
  </read_first>
  <action>
    Create stub modules with **inline types** (no separate `src/types/course.ts`):
    - `courseProjects.ts` — `Project` type per DATA_CONTRACT §3.3; `export const courseProjects: Project[] = []`
    - `courseFaq.ts` — `FAQItem` type; `export const courseFaq: FAQItem[] = []`
    - `courseRegistration.ts` — `CourseRegistrationConfig` mirroring sponsor `googleFormLinks` shape; use `googleFormLinks: { vi: "", en: "" }` and `messengerUrl: "https://m.me/roboticssocson"`; minimal bilingual title/subtitle strings for Phase 5 UI
    - `courseSections.ts` — `CourseCard` type; export empty arrays: `courseProblemCards`, `courseSolutionCards`, `courseOutcomeCards`, `courseMethodSteps`, `courseLevelingCards`, `courseAiUsageCards` (names per technical spec section blobs)
    Update `src/data/index.ts` to add:
    `export * from "./courseHero"`, `courseCurriculum`, `courseProjects`, `courseFaq`, `courseRegistration`, `courseSections`.
  </action>
  <verify>
    <automated>npm run build</automated>
    <automated>rg "courseHero|courseCurriculum|courseProjects" src/data/index.ts</automated>
  </verify>
  <acceptance_criteria>
    <criterion>All six `course*.ts` modules exist and compile under TypeScript strict mode.</criterion>
    <criterion>`src/data/index.ts` re-exports every new course module.</criterion>
    <criterion>Stub files use empty arrays where content is Phase 4 scope — no TypeScript errors.</criterion>
  </acceptance_criteria>
</task>

</tasks>

<must_haves>
<truths>
- D-DATA-01: Split `courseHero`, `courseCurriculum`, `courseProjects`, `courseFaq`, `courseRegistration`, `courseSections` data files exist.
- D-DATA-02: Types are inline per data file; no `src/types/course.ts`.
- D-DATA-03: All course modules exported from `src/data/index.ts`.
- D-CURR-01: `courseCurriculum.ts` exports exactly 12 lessons.
- D-CURR-02: Curriculum uses nested `LocalizedText` for bilingual fields.
- D-CURR-03: Each lesson has id, title, goal, product, primaryLevel, secondaryLevel, challenge.
- D-CURR-04: No placeholder strings in curriculum data.
- D-CURR-05: Phase 3 creates curriculum data only — no `CourseCurriculum` UI component.
- DATA-01 / DATA-02 / I18N-01: Typed bilingual static data compiles and supports locale rendering.
- `npm run build` passes with all new data modules.
</truths>
</must_haves>

## PLAN COMPLETE
