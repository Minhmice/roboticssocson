---
phase: 3
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - "src/lib/seo/metadata.ts"
  - "src/lib/seo/course-seo.ts"
  - "src/app/course/layout.tsx"
  - "src/app/layout.tsx"
autonomous: true
requirements:
  - QUAL-01
---

# Plan 03-01: SEO Registry and Course Route Metadata

## must_haves

- D-SEO-01: Root layout global defaults + title.template only
- D-SEO-02: Route-level generateMetadata in src/app/course/layout.tsx
- D-SEO-03: src/lib/seo/metadata.ts + course-seo.ts registry
- D-SEO-04: No client-side SEO patching on /course
- D-SEO-05: courseSeo.vi as default server metadata
- D-SEO-06: Defer /en/course URL segment; registry holds en strings

<objective>
Introduce typed server-side SEO infrastructure (`src/lib/seo/`) and route-level metadata for `/course` so the course page owns its title, description, and Open Graph — without client-side `document.title` patching.
</objective>

## Context Coverage

- D-SEO-01: Root layout global defaults + `title.template` only
- D-SEO-02: `/course` owns SEO via `src/app/course/layout.tsx`
- D-SEO-03: `RouteSeo` type + `buildMetadata` factory + `courseSeo` registry
- D-SEO-04: No client-side SEO patching for `/course`
- D-SEO-05: Vietnamese default metadata for `/course` in Phase 3
- D-SEO-06: `/en/course` URL segment deferred — registry holds `en` strings only

## Artifacts this phase produces

- `src/lib/seo/metadata.ts` — `Locale`, `RouteSeo`, `buildMetadata()`
- `src/lib/seo/course-seo.ts` — `courseSeo: Record<Locale, RouteSeo>`
- `src/app/course/layout.tsx` — `generateMetadata()` using `courseSeo.vi`
- Root `src/app/layout.tsx` — optional `title.template` refinement (non-breaking)

<tasks>

<task id="01" type="execute">
  <title>Create SEO metadata factory</title>
  <read_first>
    <file>src/app/layout.tsx</file>
    <file>.planning/phases/03-course-data-route-foundation/03-CONTEXT.md</file>
    <file>docs/course/technical_SPEC.md</file>
  </read_first>
  <action>
    Create `src/lib/seo/metadata.ts` exporting:
    - `export type Locale = "vi" | "en"`
    - `export type RouteSeo` with fields: `title`, `description`, optional `keywords`, `canonicalPath`, optional `ogTitle`, `ogDescription`, `ogImage`
    - `export function buildMetadata(seo: RouteSeo): Metadata` importing `Metadata` from `next`
    Factory must set: `title`, `description`, `keywords` (when provided), `alternates.canonical` from `seo.canonicalPath`, `openGraph.title` (ogTitle ?? title), `openGraph.description` (ogDescription ?? description), `openGraph.type: "website"`, `openGraph.images` from `ogImage` defaulting to `/Logo/RBS Logo.svg` with width 637 height 483 alt "Robotics Sóc Sơn Logo", and `twitter.card: "summary_large_image"` with matching title/description/images.
    Do not import React or client hooks in this file.
  </action>
  <verify>
    <automated>test -f src/lib/seo/metadata.ts</automated>
    <automated>rg "export function buildMetadata" src/lib/seo/metadata.ts</automated>
    <automated>rg "export type RouteSeo" src/lib/seo/metadata.ts</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`metadata.ts` exports `Locale`, `RouteSeo`, and `buildMetadata` with no `"use client"` directive.</criterion>
    <criterion>`buildMetadata` returns Next.js `Metadata` including `alternates.canonical` and Open Graph fields.</criterion>
  </acceptance_criteria>
</task>

<task id="02" type="execute">
  <title>Create course SEO registry</title>
  <read_first>
    <file>src/lib/seo/metadata.ts</file>
    <file>docs/course/PRD.md</file>
    <file>.planning/phases/03-course-data-route-foundation/03-CONTEXT.md</file>
  </read_first>
  <action>
    Create `src/lib/seo/course-seo.ts` exporting `courseSeo: Record&lt;Locale, RouteSeo&gt;` with:
    - `vi.title`: `Từ Khối Lệnh Đến Phần Cứng | Robotics Sóc Sơn`
    - `vi.description`: concise Vietnamese STEM course summary mentioning Scratch, mBlock, Arduino, grades 1–2, 12 sessions (1–2 sentences, no placeholder text)
    - `vi.canonicalPath`: `/course`
    - `en.title`: `From Blocks to Hardware | Robotics Sóc Sơn`
    - `en.description`: English equivalent of the Vietnamese description
    - `en.canonicalPath`: `/course`
  Both locales use `ogImage: "/Logo/RBS Logo.svg"` until course OG asset exists.
  </action>
  <verify>
    <automated>test -f src/lib/seo/course-seo.ts</automated>
    <automated>rg "courseSeo" src/lib/seo/course-seo.ts</automated>
    <automated>rg "Từ Khối Lệnh Đến Phần Cứng" src/lib/seo/course-seo.ts</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`courseSeo` has both `vi` and `en` entries with non-empty `title` and `description`.</criterion>
    <criterion>Neither entry uses placeholder strings TBD, lorem ipsum, or coming soon.</criterion>
  </acceptance_criteria>
</task>

<task id="03" type="execute">
  <title>Wire course layout metadata and refine root template</title>
  <read_first>
    <file>src/lib/seo/course-seo.ts</file>
    <file>src/lib/seo/metadata.ts</file>
    <file>src/app/layout.tsx</file>
    <file>src/hooks/useDynamicMetadata.ts</file>
  </read_first>
  <action>
    Create `src/app/course/layout.tsx` as a **server** component (no `"use client"`):
    - `import { buildMetadata } from "@/lib/seo/metadata"`
    - `import { courseSeo } from "@/lib/seo/course-seo"`
    - `export function generateMetadata()` returning `buildMetadata(courseSeo.vi)`
    - Default export passes `{children}` through unchanged
    Do NOT add `useDynamicMetadata` or any client metadata hook under `/course`.
    In `src/app/layout.tsx`, change root `metadata.title` from a bare string to `{ default: "RBS - Sponsorship Site", template: "%s | Robotics Sóc Sơn" }` **only if** course SEO titles are not double-suffixed; if `buildMetadata` already includes full title with suffix, keep root title as-is and document in code comment. Prefer: course `RouteSeo.title` is the full page title string; root keeps `title.template` for future child routes.
  </action>
  <verify>
    <automated>test -f src/app/course/layout.tsx</automated>
    <automated>rg "generateMetadata" src/app/course/layout.tsx</automated>
    <automated>rg "use client" src/app/course/layout.tsx ; test $? -ne 0</automated>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`/course` route compiles with dedicated `layout.tsx` exporting `generateMetadata`.</criterion>
    <criterion>Production build succeeds after SEO files are added.</criterion>
    <criterion>Course layout does not import or extend `useDynamicMetadata`.</criterion>
  </acceptance_criteria>
</task>

</tasks>

<must_haves>
<truths>
- D-SEO-01: Root `layout.tsx` keeps global defaults; course title not hardcoded at root.
- D-SEO-02: `/course` owns SEO via `generateMetadata` in `src/app/course/layout.tsx`.
- D-SEO-03: Typed SEO registry at `src/lib/seo/metadata.ts` + `course-seo.ts` with `buildMetadata`.
- D-SEO-04: No client-side SEO patching (`useEffect`, `document.title`, `useDynamicMetadata`) on `/course`.
- D-SEO-05: Server metadata for `/course` uses `courseSeo.vi` as default.
- D-SEO-06: No `[locale]` URL refactor; `courseSeo.en` exists for future routing only.
- `/course` server metadata is course-specific (title contains course name, not generic CLB-only sponsorship title).
</truths>
<prohibitions>
- statement: Do not extend `useDynamicMetadata` or add `useEffect` title patching for `/course`
  status: resolved
  verification: `rg "useDynamicMetadata" src/app/course/` exits non-zero
- statement: Do not refactor root app to `[locale]` URL segments in Phase 3
  status: resolved
  verification: No `src/app/[locale]` directory created
</prohibitions>
</must_haves>

## PLAN COMPLETE
