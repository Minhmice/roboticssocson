---
phase: 9
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - "src/data/arduinoMblockDeck.ts"
  - "src/data/arduinoMblockDeckWireframe.ts"
  - "src/data/index.ts"
autonomous: true
requirements:
  - DECK-02
---

# Plan 09-01: Bilingual deck data layer

## must_haves

- D-DATA-01: Exactly 30 slides in production data module
- D-DATA-02: All user-facing strings use `LocalizedText` with `vi` and `en`
- D-DATA-04: Layout union preserved from wireframe types

<objective>
Create `src/data/arduinoMblockDeck.ts` with typed bilingual slide content for all 30 slides, migrated from the wireframe data file. Export `DECK_TOTAL_SLIDES` and slide array for the deck player.
</objective>

<tasks>

<task id="01" type="execute">
  <title>Define production deck types and exports</title>
  <read_first>
    <file>src/data/arduinoMblockDeckWireframe.ts</file>
    <file>src/lib/course/getLocalized.ts</file>
    <file>docs/course/arduino-mblock-deck/WIREFRAME.md</file>
    <file>.planning/phases/09-arduino-mblock-presentation-deck/09-CONTEXT.md</file>
  </read_first>
  <action>
    Create `src/data/arduinoMblockDeck.ts`:
    - Import `LocalizedText` from existing course types or define inline matching `getLocalized` usage.
    - Export `DeckSlideLayout` union (same 11 values as wireframe).
    - Export `DeckSlide` interface with localized fields: `title`, `subtitle?`, `section?`, `bullets?`, `left?`/`right?` with `label: LocalizedText` and `items: LocalizedText[]`, `diagram?: LocalizedText[]`, `mediaAlt?: LocalizedText[]` (optional alt for placeholders).
    - Export `arduinoMblockDeckSlides: DeckSlide[]` (30 entries) and `DECK_TOTAL_SLIDES = 30`.
    - Re-export types from `src/data/index.ts` if that barrel exists for course data.
  </action>
  <acceptance_criteria>
    <criterion>`arduinoMblockDeck.ts` exports `arduinoMblockDeckSlides` array with length exactly 30.</criterion>
    <criterion>Every slide `title` has both `vi` and `en` non-empty strings.</criterion>
    <criterion>`DeckSlideLayout` includes all 11 layout values from wireframe spec.</criterion>
  </acceptance_criteria>
</task>

<task id="02" type="execute">
  <title>Migrate VI copy and add EN translations</title>
  <read_first>
    <file>src/data/arduinoMblockDeckWireframe.ts</file>
    <file>src/data/arduinoMblockDeck.ts</file>
    <file>docs/course/arduino-mblock-deck/WIREFRAME.md</file>
  </read_first>
  <action>
    For each of the 30 wireframe slides, copy Vietnamese strings into `vi` fields and provide English `en` translations for: title, subtitle, section, bullets, column labels/items, diagram lines, mediaAlt.
    Preserve slide `id` 1–30 and `layout` values matching WIREFRAME.md table.
    Content must cover all four user brief sections (Arduino basics, teaching advantages, synergy, kids).
    Keep `arduinoMblockDeckWireframe.ts` temporarily but add deprecation comment pointing to `arduinoMblockDeck.ts` (remove in 09-02).
  </action>
  <acceptance_criteria>
    <criterion>Slide ids 1 through 30 are contiguous with layouts matching WIREFRAME.md slide map.</criterion>
    <criterion>Slide 1 title vi contains "Arduino" and "mBlock"; slide 30 references course brand "Từ Khối Lệnh Đến Phần Cứng".</criterion>
    <criterion>No slide uses empty string for required `title.vi` or `title.en`.</criterion>
  </acceptance_criteria>
</task>

</tasks>

## verification

```bash
node -e "const m=require('./src/data/arduinoMblockDeck.ts');" 2>/dev/null || npx tsx -e "import { arduinoMblockDeckSlides, DECK_TOTAL_SLIDES } from './src/data/arduinoMblockDeck'; if (arduinoMblockDeckSlides.length !== 30 || DECK_TOTAL_SLIDES !== 30) process.exit(1)"
npm run lint -- --max-warnings=0 src/data/arduinoMblockDeck.ts
```
