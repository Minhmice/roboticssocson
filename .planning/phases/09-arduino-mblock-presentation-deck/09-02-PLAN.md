---
phase: 9
plan: "02"
type: execute
wave: 2
depends_on:
  - "09-01"
files_modified:
  - "src/components/course/deck/DeckPlayer.tsx"
  - "src/components/course/deck/DeckSlide.tsx"
  - "src/app/course/arduino-mblock-deck/page.tsx"
  - "src/data/arduinoMblockDeckWireframe.ts"
autonomous: true
requirements:
  - DECK-01
  - DECK-03
  - DECK-04
---

# Plan 09-02: Production deck player and slide layouts

## must_haves

- D-NAV-01 through D-NAV-05: Left/right tap zones, keyboard nav, progress, disabled ends
- D-UX-01: No wireframe dashed placeholders or WIREFRAME labels in production UI
- D-UX-02: Light theme semantic tokens (`foreground`, `primary`, `muted`, `border`)
- D-ARCH-01: Deck logic in `src/components/course/deck/`

<objective>
Extract and restyle the wireframe page into production `DeckPlayer` + `DeckSlide` components using bilingual data from 09-01. Single-slide viewport with PPTX-style presentation chrome.
</objective>

<tasks>

<task id="01" type="execute">
  <title>Create DeckPlayer with navigation chrome</title>
  <read_first>
    <file>src/app/course/arduino-mblock-deck/page.tsx</file>
    <file>src/data/arduinoMblockDeck.ts</file>
    <file>DESIGN.md</file>
    <file>.planning/phases/09-arduino-mblock-presentation-deck/09-CONTEXT.md</file>
  </read_first>
  <action>
    Create `src/components/course/deck/DeckPlayer.tsx` as `"use client"`:
    - Props: `slides: DeckSlide[]`, optional `exitHref` default `/course`, optional `deckTitle` LocalizedText.
    - State: `index` 0..slides.length-1; `goPrev`/`goNext` callbacks.
    - Left 18% and right 18% `<button type="button">` tap zones with `aria-label` vi/en from `useLanguage`.
    - Keyboard listener for ArrowLeft/ArrowRight with preventDefault.
    - Top bar: deck title (muted), counter `{index+1} / {total}`, Link "Thoát"/"Exit" to exitHref.
    - Bottom: progressbar with `role="progressbar"`, primary fill width percent.
    - Footer hint text for tap/keyboard (bilingual via `t()` or inline LocalizedText).
    - Use `min-h-[calc(100dvh-4rem)]`, semantic colors — no `slate-200` wireframe labels.
    - Respect `prefers-reduced-motion`: progress width transition `motion-reduce:transition-none`.
  </action>
  <acceptance_criteria>
    <criterion>`DeckPlayer.tsx` exists and exports `DeckPlayer` component.</criterion>
    <criterion>File contains `w-[18%]` tap zone buttons and `ArrowLeft` keydown handler.</criterion>
    <criterion>File does not contain string `WIREFRAME` or `border-dashed border-slate-400` wireframe placeholder styling.</criterion>
  </acceptance_criteria>
</task>

<task id="02" type="execute">
  <title>Create DeckSlide layout renderer</title>
  <read_first>
    <file>src/app/course/arduino-mblock-deck/page.tsx</file>
    <file>src/data/arduinoMblockDeck.ts</file>
    <file>src/lib/course/getLocalized.ts</file>
    <file>src/components/shared/MediaPlaceholder.tsx</file>
  </read_first>
  <action>
    Create `src/components/course/deck/DeckSlide.tsx`:
    - Props: `slide: DeckSlide`, `locale` from `useLanguage`.
    - Resolve all text via `getLocalized(field, locale)`.
    - Implement all 11 layouts with production styling:
      - `title`: hero typography, optional MediaPlaceholder if mediaAlt present
      - `section`: centered large heading, optional `bg-accent/40` band
      - `headline-bullets`, `two-column`, `grid-4`, `grid-3`, `diagram`, `split-media`, `example`, `summary`, `closing`
    - Diagram layout: use `rounded-xl border border-border bg-muted` monospace block — not dashed wireframe.
    - Grid cells: use `MediaPlaceholder` with localized alt when `mediaAlt` provided.
    - Max prose width ~65ch for bullet lists (`max-w-prose` or `max-w-3xl`).
  </action>
  <acceptance_criteria>
    <criterion>`DeckSlide.tsx` switches on `slide.layout` for all 11 layout values.</criterion>
    <criterion>Component imports and uses `getLocalized` for slide title rendering.</criterion>
    <criterion>No `WireframeLabel` component or equivalent wireframe badge in file.</criterion>
  </acceptance_criteria>
</task>

<task id="03" type="execute">
  <title>Wire page to DeckPlayer and remove wireframe data import</title>
  <read_first>
    <file>src/app/course/arduino-mblock-deck/page.tsx</file>
    <file>src/components/course/deck/DeckPlayer.tsx</file>
    <file>src/data/arduinoMblockDeck.ts</file>
  </read_first>
  <action>
    Refactor `src/app/course/arduino-mblock-deck/page.tsx` to thin compose:
    - Import `arduinoMblockDeckSlides` from `arduinoMblockDeck.ts`.
    - Render `<DeckPlayer slides={arduinoMblockDeckSlides} />`.
    - Delete inline `SlideBody`, `WireframeLabel`, `MediaPlaceholder` from page file.
    - Delete or empty `src/data/arduinoMblockDeckWireframe.ts` if fully superseded (or re-export from new file for one commit — prefer delete if no imports remain).
    - Run ripgrep to ensure no imports of `arduinoMblockDeckWireframe` remain.
  </action>
  <acceptance_criteria>
    <criterion>`page.tsx` is under 40 lines and imports `DeckPlayer`.</criterion>
    <criterion>`rg arduinoMblockDeckWireframe src` returns no matches after cleanup.</criterion>
  </acceptance_criteria>
</task>

</tasks>

## verification

```bash
npm run lint -- src/components/course/deck/ src/app/course/arduino-mblock-deck/
rg "WIREFRAME|arduinoMblockDeckWireframe" src/
```
