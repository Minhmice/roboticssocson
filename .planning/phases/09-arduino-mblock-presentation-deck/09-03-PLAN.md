---
phase: 9
plan: "03"
type: execute
wave: 3
depends_on:
  - "09-02"
files_modified:
  - "src/app/course/arduino-mblock-deck/layout.tsx"
  - "src/app/course/CourseMethod.tsx"
  - "messages/vi.json"
  - "messages/en.json"
  - "docs/course/arduino-mblock-deck/WIREFRAME.md"
autonomous: true
requirements:
  - DECK-05
  - QUAL-01
---

# Plan 09-03: Course integration, metadata, and verification

## must_haves

- D-LINK-01: Discoverable link from `/course` to deck
- D-LINK-02: Production SEO metadata (not "Wireframe" title)
- QUAL-01: lint passes on touched files

<objective>
Link the deck from the course surface, update metadata and message keys, mark wireframe doc status, and verify build/lint for phase 9 deliverables.
</objective>

<tasks>

<task id="01" type="execute">
  <title>Update deck route metadata</title>
  <read_first>
    <file>src/app/course/arduino-mblock-deck/layout.tsx</file>
    <file>src/lib/seo/metadata.ts</file>
    <file>src/lib/seo/course-seo.ts</file>
  </read_first>
  <action>
    Update `generateMetadata` in `layout.tsx`:
    - title vi: "Arduino & mBlock — Trình chiếu Robotics" (or equivalent bilingual via buildMetadata single locale — match course pattern)
    - description: educational deck for students, 30 slides
    - canonicalPath: `/course/arduino-mblock-deck`
    - Remove substring "Wireframe" from title and description.
  </action>
  <acceptance_criteria>
    <criterion>`layout.tsx` metadata title does not contain "Wireframe".</criterion>
    <criterion>`canonicalPath` equals `/course/arduino-mblock-deck`.</criterion>
  </acceptance_criteria>
</task>

<task id="02" type="execute">
  <title>Add course page link to presentation deck</title>
  <read_first>
    <file>src/app/course/CourseMethod.tsx</file>
    <file>messages/vi.json</file>
    <file>messages/en.json</file>
    <file>.planning/phases/09-arduino-mblock-presentation-deck/09-CONTEXT.md</file>
  </read_first>
  <action>
    Add discoverability on `/course`:
    - Add message keys `course.deckLink` and `course.deckLinkDescription` in vi.json and en.json.
    - In `CourseMethod.tsx` (or end of Method section), add a compact callout with Link to `/course/arduino-mblock-deck` using `CTAButton` variant secondary or text link with 44px min touch target.
    - Copy references Arduino + mBlock presentation for students/parents.
    - Style with light theme tokens (`border-border`, `bg-accent/50`) — no GlowCard side-stripe accent.
  </action>
  <acceptance_criteria>
    <criterion>`messages/vi.json` contains key `course.deckLink` with non-empty string.</criterion>
    <criterion>`CourseMethod.tsx` contains href `/course/arduino-mblock-deck`.</criterion>
  </acceptance_criteria>
</task>

<task id="03" type="execute">
  <title>Update wireframe doc status and verify</title>
  <read_first>
    <file>docs/course/arduino-mblock-deck/WIREFRAME.md</file>
    <file>.planning/phases/09-arduino-mblock-presentation-deck/09-CONTEXT.md</file>
  </read_first>
  <action>
    Update `WIREFRAME.md` header status from "Wireframe v1" to note production implementation complete (reference `DeckPlayer`).
    Run verification:
    - `npm run lint`
    - `npm run check:assets` (if deck adds image paths)
    - Manual checklist: open `/course/arduino-mblock-deck`, advance through slides 1, 15, 30 via tap zones and keyboard; toggle language on slide with bullets.
    Document any `npm run build` failure from unrelated posthog-js in phase notes only — do not block deck on Phase 8 dep.
  </action>
  <acceptance_criteria>
    <criterion>`npm run lint` exits 0.</criterion>
    <criterion>`WIREFRAME.md` status line mentions production deck shipped.</criterion>
  </acceptance_criteria>
</task>

</tasks>

## verification

```bash
npm run lint
npm run check:assets
rg "Wireframe" src/app/course/arduino-mblock-deck/layout.tsx && exit 1 || exit 0
```
