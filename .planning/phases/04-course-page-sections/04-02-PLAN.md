---
phase: 4
plan: "02"
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - "src/app/course/CourseProblem.tsx"
  - "src/app/course/CourseSolution.tsx"
  - "src/app/course/CourseOutcomes.tsx"
  - "src/app/course/CourseMethod.tsx"
  - "src/app/course/CourseLeveling.tsx"
  - "src/app/course/CourseAIUsage.tsx"
  - "src/lib/course/lucideFromName.ts"
autonomous: true
requirements:
  - PAGE-02
  - PAGE-05
  - DSGN-04
---

# Plan 04-02: Trust and Narrative Sections

## must_haves

- D-RHYTHM-01: PRD section order for Problem → Solution → Outcomes → Method → Leveling → AI
- D-RHYTHM-02: Trust-first — strong parent-facing hierarchy in Problem/Solution/Outcomes
- D-RHYTHM-04: 3-column grid on lg for card sections
- D-UX-01: Mixed surfaces — not identical card grid on every section
- D-UX-02: Sparse section badges
- D-UX-03: Competition-grade STEM dark arena tone
- D-UX-04: FadeInSection on each section

<objective>
Implement Problem, Solution, Outcomes, Method, Leveling, and AI sections with varied layouts, bilingual data, and impeccable anti-pattern compliance (mixed surfaces, sparse badges).
</objective>

## Artifacts this phase produces

- `src/app/course/CourseProblem.tsx` — `id="course-problem"`
- `src/app/course/CourseSolution.tsx` — `id="course-solution"`
- `src/app/course/CourseOutcomes.tsx` — `id="course-outcomes"`
- `src/app/course/CourseMethod.tsx` — `id="course-method"`
- `src/app/course/CourseLeveling.tsx` — `id="course-leveling"`
- `src/app/course/CourseAIUsage.tsx` — `id="course-ai"`
- `src/lib/course/lucideFromName.ts` — map icon string to lucide component

<tasks>

<task id="01" type="execute">
  <title>Lucide icon mapper helper</title>
  <read_first>
    <file>src/data/courseSections.ts</file>
    <file>lucide-react package usage in src/components</file>
  </read_first>
  <action>
    Create `src/lib/course/lucideFromName.ts` exporting `getLucideIcon(name: string): LucideIcon` with a typed map of allowed icon names used in courseSections data (e.g. `AlertTriangle`, `Lightbulb`, `Cpu`, `Workflow`, `Bot`, `Target`, `GraduationCap`). Fallback to `Circle` if unknown. No dynamic `import()` — static map only.
  </action>
  <verify>
    <automated>test -f src/lib/course/lucideFromName.ts</automated>
  </verify>
  <acceptance_criteria>
    <criterion>Helper returns a valid Lucide component for each icon name in courseSections data.</criterion>
  </acceptance_criteria>
</task>

<task id="02" type="execute">
  <title>CourseProblem, CourseSolution, CourseOutcomes</title>
  <read_first>
    <file>src/components/shared/SectionHeader.tsx</file>
    <file>src/components/shared/GlowCard.tsx</file>
    <file>src/components/shared/FadeInSection.tsx</file>
    <file>src/components/sponsor/WhySponsor.tsx</file>
    <file>src/data/courseSections.ts</file>
  </read_first>
  <action>
    Create three `"use client"` section components wrapped in `FadeInSection`:
    - **CourseProblem** (`id="course-problem"`): `SectionHeader` with optional badge on Problem only; 3-column `lg:grid-cols-3` grid of pit-slate bordered cards (NOT GlowCard on every item — use `rounded-2xl border border-slate-800 bg-slate-900/30`); lucide icon + title + description via `getLocalized`.
    - **CourseSolution** (`id="course-solution"`): **different layout** — vertical prose block with icon list (check or arrow bullets), no 3-card clone of Problem; emphasize solution narrative.
    - **CourseOutcomes** (`id="course-outcomes"`): use `GlowCard` for 6 outcome highlights in `lg:grid-cols-3` grid — metric-style emphasis for parent scan (D-RHYTHM-02).
    Spacing: `py-12 sm:py-16 md:py-24`, `max-w-7xl mx-auto px-4 sm:px-6 md:px-8`.
    Body text `text-foreground`, supporting `text-muted-foreground`, min 44px touch targets if interactive.
  </action>
  <verify>
    <automated>test -f src/app/course/CourseProblem.tsx &amp;&amp; test -f src/app/course/CourseSolution.tsx &amp;&amp; test -f src/app/course/CourseOutcomes.tsx</automated>
    <automated>rg 'id="course-problem"' src/app/course/CourseProblem.tsx</automated>
  </verify>
  <acceptance_criteria>
    <criterion>Three trust sections render bilingual copy from courseSections data.</criterion>
    <criterion>Problem and Solution use visibly different layout patterns (D-UX-01).</criterion>
    <criterion>No gradient text, no side-stripe borders, no numbered 01/02/03 eyebrows.</criterion>
  </acceptance_criteria>
</task>

<task id="03" type="execute">
  <title>CourseMethod, CourseLeveling, CourseAIUsage</title>
  <read_first>
    <file>src/data/courseSections.ts</file>
    <file>.planning/phases/04-course-page-sections/04-CONTEXT.md</file>
  </read_first>
  <action>
    Create three section components:
    - **CourseMethod** (`id="course-method"`): numbered step rail (1–7) — the ONE place numbered steps appear (legitimate sequence per impeccable); vertical on mobile, optional horizontal scroll on md+; pit-slate step rows.
    - **CourseLeveling** (`id="course-leveling"`): two-column comparison (Cấp 1 vs Cấp 2) using side-by-side panels, not a 3-card grid.
    - **CourseAIUsage** (`id="course-ai"`): bullet list with lucide icons in a single `GlowCard` container or split 2-column list — concise, no cartoon styling.
    All use `FadeInSection`, `SectionHeader` without badge on every section (sparse — badge optional on Method only).
  </action>
  <verify>
    <automated>test -f src/app/course/CourseMethod.tsx &amp;&amp; test -f src/app/course/CourseLeveling.tsx &amp;&amp; test -f src/app/course/CourseAIUsage.tsx</automated>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    <criterion>Method, Leveling, AI sections render from static data with locale toggle.</criterion>
    <criterion>All six narrative section files compile and build passes.</criterion>
  </acceptance_criteria>
</task>

</tasks>

<must_haves>
<truths>
- D-RHYTHM-01: Problem, Solution, Outcomes, Method, Leveling, AI components exist with stable section ids.
- D-RHYTHM-02: Outcomes section uses visual emphasis suitable for parent 30-second scan.
- D-RHYTHM-04: Card grids use lg:grid-cols-3 where specified.
- D-UX-01: Mixed surface layouts across Problem/Solution/Outcomes/Method/Leveling/AI.
- D-UX-02: Sparse badges — not every SectionHeader has badge.
- D-UX-03: Dark arena tokens only; no light PRD theme.
- D-UX-04: Each section wrapped in FadeInSection.
- PAGE-02 and PAGE-05 content requirements satisfied by these six sections.
</truths>
</must_haves>

## PLAN COMPLETE
