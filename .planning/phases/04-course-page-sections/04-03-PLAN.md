---
phase: 4
plan: "03"
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - "src/app/course/CourseCurriculum.tsx"
  - "src/app/course/CourseProjects.tsx"
  - "src/app/course/CourseMidPageCTA.tsx"
autonomous: true
requirements:
  - PAGE-03
  - PAGE-04
---

# Plan 04-03: Curriculum, Projects, and Mid-Page CTA

## must_haves

- D-CURR-UI-01: Vertical expandable list for 12 lessons
- D-CURR-UI-02: Expanded row shows all lesson fields
- D-CURR-UI-03: Single-open accordion (useState)
- D-CURR-UI-04: Part badge on each row
- D-PROJ-01: Two-column GlowCard grid on lg+
- D-PROJ-02: Full project breakdown in each card
- D-PROJ-03: MediaPlaceholder without asset path
- D-RHYTHM-03: Mid-page CTA band after Curriculum, before FAQ
- D-UX-05: Pit-slate curriculum rows; cyan on hover/open only
- D-UX-07: Mid-page CTA slate band, no AuroraBackground

<objective>
Implement the curriculum accordion, two capstone project cards, and the mid-page slate CTA band between curriculum and FAQ.
</objective>

## Artifacts this phase produces

- `src/app/course/CourseCurriculum.tsx` — `id="course-curriculum"`
- `src/app/course/CourseProjects.tsx` — `id="course-projects"`
- `src/app/course/CourseMidPageCTA.tsx` — full-width slate band (no section id required)

<tasks>

<task id="01" type="execute">
  <title>CourseCurriculum accordion</title>
  <read_first>
    <file>src/data/courseCurriculum.ts</file>
    <file>src/lib/course/getLocalized.ts</file>
    <file>docs/course/technical_SPEC.md</file>
    <file>DESIGN.md</file>
  </read_first>
  <action>
    Create `CourseCurriculum.tsx` as `"use client"` component:
    - `id="course-curriculum"` on outer `FadeInSection`
  - `SectionHeader` with badge on Curriculum (sparse badge — allowed per D-UX-02)
    - Map `courseLessons` to vertical list of expandable rows
    - `useState<string | null>` for `openLessonId` — single-open: clicking open row closes previous
    - Row header: lesson number, part badge (`Scratch` | `Hardware` | `Capstone` from `lesson.part`), title, chevron
    - Row styles: `border border-slate-800 bg-slate-900/30 rounded-xl`; open/hover: `border-primary/30` cyan signal only on interaction (D-UX-05)
    - Expanded panel: labeled fields Goal, Product, Cấp 1/primaryLevel, Cấp 2/secondaryLevel, Challenge — all via `getLocalized`
    - Do NOT use Timeline, tabs, or 12× GlowCard grid
    - `aria-expanded` on toggle button; keyboard accessible
  </action>
  <verify>
    <automated>rg 'id="course-curriculum"' src/app/course/CourseCurriculum.tsx</automated>
    <automated>rg "useState" src/app/course/CourseCurriculum.tsx</automated>
    <automated>rg "courseLessons" src/app/course/CourseCurriculum.tsx</automated>
  </verify>
  <acceptance_criteria>
    <criterion>12 lessons render from courseLessons with part badges.</criterion>
    <criterion>Only one lesson expanded at a time; all five detail fields visible when expanded.</criterion>
  </acceptance_criteria>
</task>

<task id="02" type="execute">
  <title>CourseProjects grid</title>
  <read_first>
    <file>src/data/courseProjects.ts</file>
    <file>src/components/shared/GlowCard.tsx</file>
    <file>src/components/shared/MediaPlaceholder.tsx</file>
  </read_first>
  <action>
    Create `CourseProjects.tsx`:
    - `id="course-projects"` on `FadeInSection`
    - `SectionHeader` with badge on Projects
    - `lg:grid-cols-2` grid of `GlowCard` per project (D-PROJ-01)
    - Each card: `MediaPlaceholder` with no `src` (D-PROJ-03), title, description, labeled lists for components / logic / skills (D-PROJ-02)
    - Bilingual via `getLocalized` for all text and list items
  </action>
  <verify>
    <automated>rg 'id="course-projects"' src/app/course/CourseProjects.tsx</automated>
    <automated>rg "MediaPlaceholder" src/app/course/CourseProjects.tsx</automated>
  </verify>
  <acceptance_criteria>
    <criterion>Two capstone projects display full breakdown in GlowCard layout.</criterion>
    <criterion>Project visuals use MediaPlaceholder without image paths.</criterion>
  </acceptance_criteria>
</task>

<task id="03" type="execute">
  <title>CourseMidPageCTA slate band</title>
  <read_first>
    <file>src/components/shared/CTAButton.tsx</file>
    <file>src/data/courseRegistration.ts</file>
    <file>.planning/phases/04-course-page-sections/04-CONTEXT.md</file>
  </read_first>
  <action>
    Create `CourseMidPageCTA.tsx` as `"use client"` component (no section id):
    - Full-width band: `bg-slate-900/50 border-y border-slate-800 py-12 md:py-16` (D-UX-07)
    - Inner `max-w-7xl` centered content
    - Bilingual headline e.g. "Sẵn sàng cho con bắt đầu?" / "Ready for your child to start?"
    - Primary `CTAButton` href `#course-register` (scroll anchor)
    - Secondary link/button to `#course-curriculum`
    - **No** AuroraBackground (D-UX-07)
    - Wrap in `FadeInSection` or static (band is between sections — motion optional)
  </action>
  <verify>
    <automated>rg "course-register|#course-register" src/app/course/CourseMidPageCTA.tsx</automated>
    <automated>rg "bg-slate-900/50" src/app/course/CourseMidPageCTA.tsx</automated>
  </verify>
  <acceptance_criteria>
    <criterion>Mid-page CTA band appears with slate styling and register + curriculum anchors.</criterion>
    <criterion>No second aurora background in CTA band.</criterion>
  </acceptance_criteria>
</task>

</tasks>

<must_haves>
<truths>
- D-CURR-UI-01 through D-CURR-UI-04: Curriculum accordion behavior and badges.
- D-PROJ-01 through D-PROJ-03: Two project GlowCards with MediaPlaceholder.
- D-RHYTHM-03: Mid-page CTA band component exists.
- D-UX-05: Pit-slate curriculum rows with cyan on interaction only.
- D-UX-07: Slate CTA band without aurora.
- PAGE-03 and PAGE-04 requirements satisfied.
</truths>
</must_haves>

## PLAN COMPLETE
