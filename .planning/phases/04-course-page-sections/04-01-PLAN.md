---
phase: 4
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - "src/data/courseSections.ts"
  - "src/data/courseProjects.ts"
  - "src/data/courseFaq.ts"
  - "src/data/courseRegistration.ts"
  - "src/components/shared/FadeInSection.tsx"
autonomous: true
requirements:
  - PAGE-02
---

# Plan 04-01: Course Section Data and FadeIn Helper

## must_haves

- D-DATA-04: Full PRD copy in courseSections, courseProjects, courseFaq
- D-DATA-05: LocalizedText for section and project data
- D-DATA-06: Lucide icon name strings on section cards
- D-FAQ-03: PRD 7 FAQ questions + 1–2 extra (8–9 total)
- D-UX-04: FadeInSection helper with prefers-reduced-motion

<objective>
Populate Phase 3 data stubs with complete bilingual PRD content using `LocalizedText`, and add a reusable `FadeInSection` wrapper for Phase 4 scroll motion (D-UX-04).
</objective>

## Artifacts this phase produces

- `src/data/courseSections.ts` — full problem/solution/outcomes/method/leveling/ai data
- `src/data/courseProjects.ts` — 2 capstone projects with LocalizedText lists
- `src/data/courseFaq.ts` — 8–9 FAQ items
- `src/components/shared/FadeInSection.tsx` — framer-motion section entrance + reduced-motion

<tasks>

<task id="01" type="execute">
  <title>Fill courseSections with PRD copy</title>
  <read_first>
    <file>docs/course/PRD.md</file>
    <file>src/data/courseSections.ts</file>
    <file>src/lib/course/getLocalized.ts</file>
    <file>.planning/phases/04-course-page-sections/04-CONTEXT.md</file>
  </read_first>
  <action>
    Refactor `courseSections.ts` types to use `LocalizedText` for title/description fields and `icon?: string` (lucide name) on `CourseCard`.
    Populate arrays with full bilingual PRD content (no placeholders):
    - `courseProblemCards`: 3 pain points from PRD § Problem
    - `courseSolutionCards`: 5 solution bullets from PRD § Solution (Scratch, flowchart, mBlock/Arduino, AI, capstone)
    - `courseOutcomeCards`: 6+ outcomes from PRD § Learning Outcomes
    - `courseMethodSteps`: 7 steps from PRD § Method (numbered flow)
    - `courseLevelingCards`: 2 entries (Cấp 1, Cấp 2) from PRD § Leveling
    - `courseAiUsageCards`: 5 bullets from PRD § AI Section + closing line "Không dùng AI để làm hộ toàn bộ"
    Export section header copy objects if needed: `courseSectionCopy` with `problem`, `solution`, `outcomes`, `method`, `leveling`, `ai` each having `title` and `subtitle` as `LocalizedText`.
  </action>
  <verify>
    <automated>rg "TBD|lorem|coming soon" src/data/courseSections.ts ; test $? -ne 0</automated>
    <automated>rg "courseProblemCards|courseOutcomeCards" src/data/courseSections.ts</automated>
  </verify>
  <acceptance_criteria>
    <criterion>All six section arrays export non-empty bilingual content from PRD.</criterion>
    <criterion>Every card uses LocalizedText fields with both vi and en populated.</criterion>
  </acceptance_criteria>
</task>

<task id="02" type="execute">
  <title>Fill courseProjects and courseFaq</title>
  <read_first>
    <file>docs/course/PRD.md</file>
    <file>src/data/courseProjects.ts</file>
    <file>src/data/courseFaq.ts</file>
  </read_first>
  <action>
    Update `CourseProject` type to use `LocalizedText` for title, description, and `LocalizedText[]` or `{ vi: string[]; en: string[] }` for components, logic, skills lists.
    Export exactly 2 projects:
    - `project-smart-bin` — Thùng rác thông minh / Smart bin
    - `project-parking-warning` — Cảnh báo lùi xe / Parking reverse warning
    Each with full description, components, logic steps, skills — bilingual concise bullets.
    Fill `courseFaq` with 8–9 items covering all 7 PRD § FAQ questions plus 1–2 extras (e.g. class size or schedule). Use `LocalizedText` for question/answer or keep inline type with `question: LocalizedText`, `answer: LocalizedText`.
    Update `courseRegistrationConfig` subtitle/title if needed for messenger bridge copy (bilingual, non-empty).
  </action>
  <verify>
    <automated>rg "project-smart-bin|project-parking" src/data/courseProjects.ts</automated>
    <automated>node -e "const fs=require('fs'); const t=fs.readFileSync('src/data/courseFaq.ts','utf8'); const m=t.match(/id:/g); if(!m||m.length&lt;8) process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`courseProjects` exports exactly 2 complete capstone projects.</criterion>
    <criterion>`courseFaq` exports 8–9 FAQ items with bilingual Q&A.</criterion>
  </acceptance_criteria>
</task>

<task id="03" type="execute">
  <title>Create FadeInSection shared wrapper</title>
  <read_first>
    <file>DESIGN.md</file>
    <file>src/components/homepage/Hero.tsx</file>
    <file>.planning/phases/04-course-page-sections/04-CONTEXT.md</file>
  </read_first>
  <action>
    Create `src/components/shared/FadeInSection.tsx` as `"use client"` component:
    - Props: `children`, optional `className`, optional `as` wrapper (default `section`)
    - Use `framer-motion` with `initial={{ opacity: 0, y: 12 }}`, `whileInView={{ opacity: 1, y: 0 }}`, `viewport={{ once: true, margin: "-50px" }}`, `transition={{ duration: 0.4, ease: [0.4, 0, 0.2, 1] }}`
    - Respect `prefers-reduced-motion`: render static wrapper without motion when `window.matchMedia('(prefers-reduced-motion: reduce)').matches` (use `useEffect` + state or CSS `@media`)
    Export from `src/components/shared/index.tsx` if that barrel exists, or import directly in course sections.
  </action>
  <verify>
    <automated>test -f src/components/shared/FadeInSection.tsx</automated>
    <automated>rg "prefers-reduced-motion" src/components/shared/FadeInSection.tsx</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`FadeInSection` wraps children with subtle viewport fade-in.</criterion>
    <criterion>Reduced-motion users see content without translate animation.</criterion>
  </acceptance_criteria>
</task>

</tasks>

<must_haves>
<truths>
- D-DATA-04: courseSections, courseProjects, courseFaq contain full PRD bilingual copy.
- D-DATA-05: Section and project data use LocalizedText pattern.
- D-DATA-06: Section cards include lucide icon name strings.
- D-FAQ-03: At least 8 FAQ entries including all 7 PRD required questions.
- FadeInSection helper exists for D-UX-04 scroll motion.
- `npm run build` passes after data type updates.
</truths>
</must_haves>

## PLAN COMPLETE
