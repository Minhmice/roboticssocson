---
phase: 6
plan: "02"
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - "src/app/course/CourseHero.tsx"
  - "src/app/course/CourseProblem.tsx"
  - "src/app/course/CourseSolution.tsx"
  - "src/components/homepage/CourseTeaser.tsx"
  - "src/lib/course/lucideFromName.ts"
autonomous: true
requirements: []
---

# Plan 06-02: Hero, Narrative Sections & Teaser UI

## objective

Render new marketing data in Hero (dual headline), Problem (4 cards + prose), Solution (prose before list), and homepage CourseTeaser subtitle — preserving dark arena layout rules.

## must_haves

<truths>
- D-MKT-HERO-01: H1 shows course headline; parentHook visible as supporting line below or as enhanced subtitle block.
- D-MKT-HERO-02: Primary CTA href remains #course-curriculum.
- D-MKT-HERO-03: Secondary CTA href remains #course-register.
- D-MKT-HERO-04: No new hero image or split layout.
- D-MKT-PROB-01..02: Four cards + pit-slate prose block below grid.
- D-MKT-SOL-01: Prose block before solution list.
- D-MKT-TEAS-01..03: Teaser subtitle from data; single CTA to /course unchanged.
- D-UX-01..05: Pit-slate prose surfaces; no second Aurora; FadeInSection preserved.
</truths>

## Artifacts this phase produces

- `CourseHero` renders `getLocalized(courseHeroData.parentHook, locale)`
- `CourseProblem` grid `lg:grid-cols-2` or `sm:grid-cols-2 lg:grid-cols-4` for 4 cards
- `CourseProblem` prose block using `courseProblemProse`
- `CourseSolution` prose block using `courseSolutionProse` above `<ul>`
- `getLucideIcon` map includes any new icon name from data (e.g. `Smartphone`, `MonitorOff`)

<tasks>

<task id="01" type="execute">
  <title>CourseHero dual headline</title>
  <read_first>
    <file>src/app/course/CourseHero.tsx</file>
    <file>src/data/courseHero.ts</file>
  </read_first>
  <action>
    After H1 headline, render parentHook as a prominent supporting paragraph (`text-lg sm:text-xl text-foreground/90 max-w-3xl`). Keep existing subtitle as technical journey line below parentHook OR merge if subtitle already sufficient — prefer distinct parentHook line per D-MKT-HERO-01. Do not change CTA hrefs or AuroraBackground usage.
  </action>
  <verify>
    <automated>rg 'parentHook' src/app/course/CourseHero.tsx</automated>
  </verify>
  <acceptance_criteria>
    <criterion>Hero displays headline + parentHook in both locales.</criterion>
    <criterion>Primary CTA still links to #course-curriculum.</criterion>
  </acceptance_criteria>
</task>

<task id="02" type="execute">
  <title>CourseProblem 4-card grid + prose</title>
  <read_first>
    <file>src/app/course/CourseProblem.tsx</file>
    <file>src/data/courseSections.ts</file>
    <file>DESIGN.md</file>
  </read_first>
  <action>
    Change grid to accommodate 4 cards: `grid gap-4 sm:gap-6 sm:grid-cols-2 lg:grid-cols-4` (or 2x2 on lg if 4-col too narrow). Below grid, add prose block: `rounded-2xl border border-slate-800 bg-slate-900/30 p-6 mt-8 max-w-3xl mx-auto text-sm sm:text-base text-muted-foreground leading-relaxed` rendering `courseProblemProse` via getLocalized. Import courseProblemProse from courseSections.
  </action>
  <verify>
    <automated>rg 'courseProblemProse' src/app/course/CourseProblem.tsx</automated>
  </verify>
  <acceptance_criteria>
    <criterion>Four problem cards render from data.</criterion>
    <criterion>Prose block visible below cards with pit-slate styling.</criterion>
  </acceptance_criteria>
</task>

<task id="03" type="execute">
  <title>CourseSolution prose + lucide + teaser</title>
  <read_first>
    <file>src/app/course/CourseSolution.tsx</file>
    <file>src/components/homepage/CourseTeaser.tsx</file>
    <file>src/lib/course/lucideFromName.ts</file>
  </read_first>
  <action>
    Insert solution prose block between SectionHeader and solution list — same pit-slate container classes as Problem prose, `max-w-3xl mx-auto mb-8`. Add any new icon names from 06-01 data to lucideFromName static map. CourseTeaser: no structural change — subtitle already reads from courseTeaserCopy (verify renders updated data after 06-01).
  </action>
  <verify>
    <automated>rg 'courseSolutionProse' src/app/course/CourseSolution.tsx</automated>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    <criterion>Solution prose appears above the 5-item list.</criterion>
    <criterion>npm run build exits 0.</criterion>
  </acceptance_criteria>
</task>

</tasks>

## PLAN COMPLETE
