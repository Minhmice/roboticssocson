---
phase: 6
plan: "03"
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - "src/app/course/CourseOffer.tsx"
  - "src/app/course/page.tsx"
  - "src/app/course/CourseOutcomes.tsx"
autonomous: true
requirements: []
---

# Plan 06-03: Offer Band, Outcomes Refresh & Verification

## objective

Wire conditional CourseOffer band before register, ensure Outcomes reflects data refresh, compose page, and verify lint/build/assets plus tone guardrails.

## must_haves

<truths>
- D-MKT-OFFER-01: CourseOffer returns null when courseOfferConfig.enabled is false.
- D-MKT-OFFER-02: CourseOffer placed after CourseFAQ and before CourseRegister in page.tsx.
- D-MKT-OFFER-03..04: Offer uses courseOffer.ts data; CTA scrolls to #course-register.
- D-MKT-OUT-01: Outcomes subtitle from refreshed courseSectionCopy.outcomes (data from 06-01).
- D-MKT-TONE-01: No banned marketing slang in rendered copy (spot-check data files).
- Phase 4–5 regression: section order unchanged except Offer insert; sticky/register/nav untouched.
</truths>

## Artifacts this phase produces

- `src/app/course/CourseOffer.tsx` — client component, slate band pattern from CourseMidPageCTA
- `page.tsx` imports CourseOffer between FAQ and Register
- Verification log in plan execution notes

<tasks>

<task id="01" type="execute">
  <title>CourseOffer conditional band</title>
  <read_first>
    <file>src/app/course/CourseMidPageCTA.tsx</file>
    <file>src/data/courseOffer.ts</file>
    <file>src/components/shared/CTAButton.tsx</file>
  </read_first>
  <action>
    Create `CourseOffer.tsx` as `"use client"` component. Read `courseOfferConfig` from data. If `!enabled`, return `null`. If enabled, render slate band matching MidPageCTA: `bg-slate-900/50 border-y border-slate-800 py-12 md:py-16`, centered headline + body from getLocalized fields, single CTAButton primary variant href="#course-register". No id anchor required (not in nav). Use useLanguage locale.
  </action>
  <verify>
    <automated>test -f src/app/course/CourseOffer.tsx</automated>
    <automated>rg 'enabled' src/app/course/CourseOffer.tsx</automated>
  </verify>
  <acceptance_criteria>
    <criterion>With enabled false, component returns null (no DOM output).</criterion>
    <criterion>CTA href is #course-register when enabled.</criterion>
  </acceptance_criteria>
</task>

<task id="02" type="execute">
  <title>Page compose + outcomes data bind</title>
  <read_first>
    <file>src/app/course/page.tsx</file>
    <file>src/app/course/CourseOutcomes.tsx</file>
    <file>src/data/courseSections.ts</file>
  </read_first>
  <action>
    Import CourseOffer in page.tsx; insert between CourseFAQ and CourseRegister. Verify CourseOutcomes already binds courseSectionCopy.outcomes and courseOutcomeCards from data — no hardcoded copy; rebuild picks up 06-01 outcome refresh automatically. Confirm full section order: Hero → Problem → Solution → Outcomes → Curriculum → Projects → Method → Leveling → AI → MidPageCTA → FAQ → Offer → Register → Sticky.
  </action>
  <verify>
    <automated>rg 'CourseOffer' src/app/course/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    <criterion>page.tsx lists CourseOffer after CourseFAQ and before CourseRegister.</criterion>
    <criterion>Section ids for nav anchors unchanged (course-curriculum, course-register, etc.).</criterion>
  </acceptance_criteria>
</task>

<task id="03" type="execute">
  <title>Lint, build, assets, tone spot-check</title>
  <read_first>
    <file>.planning/phases/06-marketing-copy-integration-parent-facing-screen-time-narrati/06-CONTEXT.md</file>
    <file>src/data/courseHero.ts</file>
    <file>src/data/courseSections.ts</file>
    <file>src/data/courseFaq.ts</file>
  </read_first>
  <action>
    Run `npm run lint`, `npm run build`, `npm run check:assets`. Grep data files for banned strings: brainrot, hack não, thuốc phiện, 1% vs 99% (case-insensitive where applicable) — must return no matches. Manual: toggle locale on /course — parent hook, prose blocks, FAQ render in EN. With offer disabled, confirm no offer band visible.
  </action>
  <verify>
    <automated>npm run lint</automated>
    <automated>npm run build</automated>
    <automated>npm run check:assets</automated>
  </verify>
  <acceptance_criteria>
    <criterion>lint, build, check:assets all exit 0.</criterion>
    <criterion>No D-MKT-TONE-01 banned phrases in src/data/course*.ts.</criterion>
    <criterion>courseFaq length 8–10.</criterion>
  </acceptance_criteria>
</task>

</tasks>

## PLAN COMPLETE
