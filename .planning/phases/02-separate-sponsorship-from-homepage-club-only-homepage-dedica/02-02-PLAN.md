---
phase: 2
plan: "02"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - "src/app/sponsorship/page.tsx"
  - "src/app/page.tsx"
  - "src/components/layout/Navbar.tsx"
  - "src/components/layout/Footer.tsx"
  - "src/app/1. competition/Hero.tsx"
  - "src/app/2. team/TheChallenge.tsx"
  - "messages/vi.json"
  - "messages/en.json"
autonomous: true
requirements:
  - HOME-01
  - HOME-02
  - HOME-03
  - SPON-01
  - SPON-02
  - SPON-03
---

# Plan 02-02: Split Routes and Realign Navigation

<objective>
Ship the live IA split: club-only `/`, dedicated `/sponsorship` pitch page, updated nav/CTAs, and intact `/sponsor` donation flow. Reuse existing section components without visual redesign.
</objective>

## Context Coverage

- D-01: Homepage club-only scope
- D-02: `/sponsorship` pitch route + `/sponsor` action route
- D-04: Nav and Hero CTA realignment
- D-05: Bilingual + brand preservation
- D-06: TheChallenge budget CTA links to `/sponsorship#budget-section`

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Dead nav anchors after section removal | medium | Replace `#budget-section` / `#why-sponsor` with `/sponsorship` routes in Navbar and Footer |
| `/sponsor` regression | medium | Do not modify `src/app/sponsor/page.tsx`; verify stepper manually after build |
| Broken cross-page hash scroll | low | Use `/sponsorship#budget-section` in TheChallenge CTA; keep `id="budget-section"` on BudgetFundraising |
</threat_model>

<tasks>

<task id="01" type="execute">
  <title>Create live sponsorship page</title>
  <read_first>
    <file>.planning/archives/sponsorship/page.snapshot.tsx</file>
    <file>src/app/page.tsx</file>
    <file>src/app/sponsor/page.tsx</file>
    <file>.planning/codebase/CONVENTIONS.md</file>
  </read_first>
  <action>
    Create `src/app/sponsorship/page.tsx` as a client page composer modeled on the sponsorship archive snapshot.
    Render sections in order: `BudgetFundraisingSection`, `BudgetBreakdownSection`, `WhySponsorSection`, `FinalCTASection`.
    Wrap in `<main className="relative scroll-smooth">` matching homepage pattern.
    Ensure `FinalCTA` and section CTAs that link to `/sponsor` remain unchanged.
  </action>
  <verify>
    <automated>test -f src/app/sponsorship/page.tsx</automated>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`/sponsorship` route builds without TypeScript errors.</criterion>
    <criterion>Page imports all four former homepage sponsorship sections.</criterion>
    <criterion>`src/app/sponsor/page.tsx` is not modified.</criterion>
  </acceptance_criteria>
</task>

<task id="02" type="execute">
  <title>Slim homepage to club introduction</title>
  <read_first>
    <file>src/app/page.tsx</file>
    <file>.planning/phases/02-separate-sponsorship-from-homepage-club-only-homepage-dedica/02-CONTEXT.md</file>
  </read_first>
  <action>
    Edit `src/app/page.tsx` to remove imports and JSX for `BudgetFundraisingSection`, `BudgetBreakdownSection`, `WhySponsorSection`, and `FinalCTASection`.
    Keep: Hero, AboutFIRST, AboutFTC, AboutSocSonHighSchool, TeamCarousel, Achievements, Mission, TheChallenge.
    Do not add new sections in this task.
  </action>
  <verify>
    <automated>rg "BudgetFundraising|BudgetBreakdown|WhySponsor|FinalCTA" src/app/page.tsx ; test $? -ne 0</automated>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`src/app/page.tsx` has zero sponsorship section imports.</criterion>
    <criterion>Club sections through TheChallenge remain in original order.</criterion>
  </acceptance_criteria>
</task>

<task id="03" type="execute">
  <title>Update Navbar and Footer links</title>
  <read_first>
    <file>src/components/layout/Navbar.tsx</file>
    <file>src/components/layout/Footer.tsx</file>
    <file>messages/vi.json</file>
    <file>messages/en.json</file>
  </read_first>
  <action>
    In Navbar `navLinks`: remove separate `#budget-section` and `#why-sponsor` anchor entries; replace with a single sponsorship route link using `href: "/sponsorship"`, `isAnchor: false` (or Next.js Link pattern already used for `/`).
    Keep achievements and about anchors on `/`.
    Navbar sponsor CTA button: set `href="/sponsorship"` (pitch first) OR keep `/sponsor` — prefer `/sponsorship` per D-02.
    In Footer quick links: change sponsorship entry from `#why-sponsor` to `/sponsorship`.
    Add message keys only if needed (e.g. `nav.sponsorshipPage`); otherwise reuse `nav.sponsorship`.
  </action>
  <verify>
    <automated>rg '#budget-section|#why-sponsor' src/components/layout/Navbar.tsx src/components/layout/Footer.tsx ; test $? -ne 0</automated>
    <automated>rg '/sponsorship' src/components/layout/Navbar.tsx src/components/layout/Footer.tsx</automated>
  </verify>
  <acceptance_criteria>
    <criterion>Navbar and Footer contain no dead homepage sponsorship anchors.</criterion>
    <criterion>At least one nav entry routes to `/sponsorship`.</criterion>
  </acceptance_criteria>
</task>

<task id="04" type="execute">
  <title>Realign Hero and TheChallenge CTAs</title>
  <read_first>
    <file>src/app/1. competition/Hero.tsx</file>
    <file>src/app/2. team/TheChallenge.tsx</file>
    <file>src/data/hero.ts</file>
  </read_first>
  <action>
    Hero: change primary CTA from `href="/sponsor"` to an on-page club action — use `href="#about-first"` with smooth scroll behavior consistent with nav, OR secondary club CTA from hero data if available.
    Keep PDF download secondary CTA if still relevant to club pitch.
    TheChallenge: replace `scrollToBudget()` targeting `#budget-section` with navigation to `/sponsorship#budget-section` using Next.js `Link` or `useRouter().push('/sponsorship#budget-section')`.
  </action>
  <verify>
    <automated>rg 'href="/sponsor"' src/app/1. competition/Hero.tsx ; test $? -ne 0</automated>
    <automated>rg '/sponsorship#budget-section' src/app/2. team/TheChallenge.tsx</automated>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    <criterion>Hero primary CTA is club-focused, not direct donation.</criterion>
    <criterion>TheChallenge budget button navigates to sponsorship page budget anchor.</criterion>
  </acceptance_criteria>
</task>

<task id="05" type="verify">
  <title>Run full verification suite</title>
  <read_first>
    <file>.planning/phases/02-separate-sponsorship-from-homepage-club-only-homepage-dedica/02-VALIDATION.md</file>
  </read_first>
  <action>
    Run `npm run lint`, `npm run build`, and `npm run check:assets`.
    Manually confirm `/`, `/sponsorship`, and `/sponsor` in dev server.
  </action>
  <verify>
    <automated>npm run lint &amp;&amp; npm run build &amp;&amp; npm run check:assets</automated>
  </verify>
  <acceptance_criteria>
    <criterion>All three npm scripts exit 0.</criterion>
    <criterion>Manual check: `/sponsor` stepper still renders corporate/personal cards.</criterion>
  </acceptance_criteria>
</task>

</tasks>

<must_haves>
<truths>
- `/` renders club introduction sections only (HOME-01).
- `/sponsorship` renders BudgetFundraising, BudgetBreakdown, WhySponsor, FinalCTA (SPON-02).
- Navbar/Footer link to `/sponsorship` instead of removed homepage anchors (HOME-02).
- `/sponsor` donation flow unchanged (SPON-03).
- Bilingual toggle works; existing design tokens unchanged (HOME-03).
</truths>
</must_haves>

## Artifacts this phase produces

- `src/app/sponsorship/page.tsx` — new sponsorship pitch route composer
- Modified `src/app/page.tsx` — club-only homepage
- Modified `src/components/layout/Navbar.tsx`
- Modified `src/components/layout/Footer.tsx`
- Modified `src/app/1. competition/Hero.tsx`
- Modified `src/app/2. team/TheChallenge.tsx`
- Optional `messages/vi.json` and `messages/en.json` nav key updates

## PLAN COMPLETE
